// Placeholder. Domain tables are defined in feature-specific mandates and
// added to this file (or split into per-feature files re-exported from here).
// Intentionally empty for the project-init mandate.

export {};
